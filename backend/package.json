{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "Multi-tier e-commerce backend service",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp src/*.js dist/",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
